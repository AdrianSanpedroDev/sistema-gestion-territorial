import { Routes } from '@angular/router';
import { AnnotationMapComponent } from './annotation-map.component';

export const ANNOTATION_MAP_ROUTES: Routes = [
  {
    path: '',
    component: AnnotationMapComponent,
  },
];
